{
  "name": "@apigate/adapter-fastify",
  "version": "0.1.0",
  "description": "Fastify plugin wrapping @apigate/core's rate limiter",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@apigate/core": "*",
    "fastify-plugin": "^6.0.0"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "fastify": "^5.12.0"
  }
}
